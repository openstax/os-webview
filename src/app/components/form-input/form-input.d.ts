export default function FormInput({label, longLabel, inputProps, suggestions}:
    {
        label: string;
        longLabel?: string;
        inputProps: object;
        suggestions?: boolean;
    }
): React.ReactNode;
