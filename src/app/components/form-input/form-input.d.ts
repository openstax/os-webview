export default function FormInput({label, longLabel, inputProps, suggestions}:
    {
        label: string;
        longLabel?: string;
        inputProps: object;
        suggestions?: string[];
    }
): React.ReactNode;
