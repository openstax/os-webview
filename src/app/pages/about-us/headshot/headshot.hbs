<div class="picture-area text-{{textColor}} bg-{{bgColor}} {{#if url}}has-picture {{/if}}{{hidden}}">
    {{#if url}}<img src="{{url}}" />{{/if}}
    <div class="name">{{name}}</div>
</div>
<div class="details">
    <div class="name">{{name}}</div>
    <div class="title">{{title}}</div>
    <div class="description mac-scroll">{{{description}}}</div>
</div>
