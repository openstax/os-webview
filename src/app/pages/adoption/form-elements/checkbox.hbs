<div class="field">
  <input id="{{id}}" name="{{name}}" type="checkbox" value="{{value}}">
  <label for="{{id}}">{{label}}</label>
  {{#if textInput}}
    <input disabled="true" id="{{textInput.id}}" name="{{textInput.name}}" type="text" size=30 />
  {{/if}}
</div>
